[build]
builder = "nixpacks"
buildCommand = "cd mathspin-backend && npm install"

[deploy]
startCommand = "cd mathspin-backend && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
